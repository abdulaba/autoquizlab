<hr>
<hr>
<hr>
<h2>Edita tu perfil de <%= resource_name.to_s.humanize %></h2>

<div class="card">
            <div class="card-body wow-bg" id="formBg">

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password,
                hint: "Dejalo en blanco si no quieres cambiarla",
                required: false,
                input_html: { autocomplete: "new-password" },
                wrapper_html: { class: 'custom-hint' }
                %>
    <%= f.input :password_confirmation,
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :current_password,
                hint: "Necesitamos que ingreses tu contraseña para guardar los cambios",
                required: true,
                input_html: { autocomplete: "current-password" },
                wrapper_html: { class: 'custom-hint' }
                %>
  </div>
</div>
</div>

<hr>
  <div class="form-actions" form class="button">
  <button>  <%= f.button :submit, "Update" %> </button>
  </div>
<% end %>
<!--
<h3>Cancel my account</h3>
<hr>
<div>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %></div>
-->
